import { inter } from "@/config/fonts";

export default function ProductsPage() {
  return (
    <div className=" items-center justify-center flex h-screen">
      <h1 className={`${inter.className} text-4xl`}>Products Page</h1>
    </div>
  );
}
